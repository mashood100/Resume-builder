import 'package:flutter/material.dart';

const borderRadius0 = BorderRadius.all(Radius.circular(0));
const borderRadius2 = BorderRadius.all(Radius.circular(2));
const borderRadius3 = BorderRadius.all(Radius.circular(3));
const borderRadius4 = BorderRadius.all(Radius.circular(4));
const borderRadius6 = BorderRadius.all(Radius.circular(6));
const borderRadius7 = BorderRadius.all(Radius.circular(7));
const borderRadius8 = BorderRadius.all(Radius.circular(8));
const borderRadius9 = BorderRadius.all(Radius.circular(9));
const borderRadius10 = BorderRadius.all(Radius.circular(10));
const borderRadius12 = BorderRadius.all(Radius.circular(12));
const borderRadius14 = BorderRadius.all(Radius.circular(14));
const borderRadius16 = BorderRadius.all(Radius.circular(16));
const borderRadius18 = BorderRadius.all(Radius.circular(18));
const borderRadius20 = BorderRadius.all(Radius.circular(20));
const borderRadius22 = BorderRadius.all(Radius.circular(22));
const borderRadius24 = BorderRadius.all(Radius.circular(24));



const borderRadiusApp24 = BorderRadius.all(Radius.circular(24));



const borderRadiusCircle = BorderRadius.all(Radius.circular(100));

